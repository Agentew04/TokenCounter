<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="TokenCounter.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:tk="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
    xmlns:local="clr-namespace:TokenCounter.Views"
    Shell.FlyoutBehavior="Disabled">
    <!-- 
        add this to shell to add nav bar: Shell.FlyoutBehavior="Flyout" 
        https://docs.microsoft.com/en-us/dotnet/maui/fundamentals/shell/flyout
    -->
    <ShellContent
        Title="Tokens"
        Icon="account_balance.svg"
        ContentTemplate="{DataTemplate local:MainPage}"
        Route="MainPage"/>
</Shell>
